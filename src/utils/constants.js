/* Constants related to the application settings and configurations. */

// Default number of sets per exercise, as it's added to the selectedExercises array
export const DEFAULT_SET_COUNT = 3;

// Default number of exercises added randomly to the selectedExercises array
export const DEFAULT_EXERCISES_COUNT = 3;

// Default time in seconds to execute one repetition of any exercise
export const DEFAULT_1_REP_DURATION = 6;

// Default time in seconds for rest after an exercise
export const DEFAULT_END_EXERCISE_REST_DURATION = 120;
